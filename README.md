# Battle for the Sword Coast — Prototype

Run a minimal static server and open the game in your browser:

```powershell
npx http-server -c-1 -p 8080 -o
```

Open http://localhost:8080/index.html

This prototype is plain JavaScript, HTML and CSS. Data files live under `public/data` and source code in `src/`.
